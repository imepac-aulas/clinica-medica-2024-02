# --- Configura��es Gerais da Aplica��o ---

# Desativa o JMX (Java Management Extensions). Em aplica��es n�o-web como esta,
# desativar o JMX ajuda a garantir que a aplica��o termine ap�s a execu��o,
# em vez de ficar rodando em segundo plano.
spring.jmx.enabled=false


# --- Configura��o do Banco de Dados (DataSource) ---

# A URL de conex�o JDBC para o banco de dados MySQL.
# O Spring Boot e o Flyway usar�o esta URL para se conectar ao banco.
spring.datasource.url=jdbc:mysql://localhost:3333/clinica_medica

# O nome de usu�rio para a conex�o com o banco de dados.
spring.datasource.username=root

# A senha para a conex�o com o banco de dados.
spring.datasource.password=root


# --- Configura��o do Flyway ---

# Habilita a cria��o de uma "linha de base" (baseline) em um banco de dados existente.
# Se o Flyway encontrar um banco com tabelas mas sem a sua tabela de hist�rico,
# ele criar� a tabela de hist�rico e marcar� o estado atual como a vers�o base.
# Essencial para introduzir o Flyway em um projeto j� existente.
spring.flyway.baseline-on-migrate=true


# --- Otimiza��o: Desativando M�dulos Desnecess�rios ---

# Exclui explicitamente as autoconfigura��es do JPA e do Hibernate.
# Este m�dulo n�o precisa de um EntityManagerFactory ou de reposit�rios,
# pois sua �nica fun��o � executar migra��es SQL. Desativ�-los torna
# a inicializa��o da aplica��o mais r�pida e leve.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
