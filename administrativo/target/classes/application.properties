# --- Configura��es do Servidor Web ---

# Define a porta em que a aplica��o web ir� rodar.
server.port=8080

# Define o nome da aplica��o, �til para logs e monitoramento.
spring.application.name=administrativo


# --- Configura��o do Banco de Dados (DataSource) ---

# A URL de conex�o JDBC para o banco de dados MySQL.
# Este m�dulo se conecta a um banco que j� se espera ter sido preparado pelo m�dulo de migra��o.
spring.datasource.url=jdbc:mysql://localhost:3333/clinica_medica

# O nome de usu�rio para a conex�o com o banco de dados.
spring.datasource.username=root

# A senha para a conex�o com o banco de dados.
spring.datasource.password=root


# --- Configura��o do JPA e Hibernate ---

# A configura��o mais importante para um m�dulo de servi�o que usa um migrador externo.
# "validate" instrui o Hibernate a N�O fazer nenhuma altera��o no esquema do banco (nem criar, nem atualizar).
# Em vez disso, ele apenas valida se as entidades (@Entity) correspondem �s tabelas existentes.
# Se houver uma incompatibilidade, a aplica��o falhar� ao iniciar, garantindo a consist�ncia.
spring.jpa.hibernate.ddl-auto=validate

# Exibe no console os comandos SQL gerados pelo Hibernate. �til para debug.
spring.jpa.show-sql=true

# Formata o SQL exibido no console para ser mais leg�vel.
spring.jpa.properties.hibernate.format_sql=true

# Especifica o dialeto SQL que o Hibernate deve usar, otimizando os comandos para o MySQL.
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# --- Configurações do Keycloak (OAuth2 Resource Server) ---

# URL do servidor Keycloak (issuer-uri)
# O Spring Boot automaticamente descobre o JWK Set URI a partir desta URL
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8180/realms/clinica-medica

